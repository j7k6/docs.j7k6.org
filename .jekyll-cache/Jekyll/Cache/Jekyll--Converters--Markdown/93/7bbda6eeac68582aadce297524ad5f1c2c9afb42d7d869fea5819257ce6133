I"l<h2 id="proxy">Proxy</h2>
<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>...

location / {
  ...
  proxy_cache &lt;$PROXY_CACHE_NAME&gt;;
  proxy_cache_lock on;
  proxy_cache_valid any 1s;
  proxy_cache_use_stale updating;
  proxy_cache_background_update on;
}

...
</code></pre></div></div>

<h2 id="fastcgi-php-fpm">FastCGI (PHP-FPM)</h2>
<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>...

location ~ \.php$ {
  ...
  fastcgi_cache &lt;$FASTCGI_CACHE_NAME&gt;;
  fastcgi_cache_lock on;
  fastcgi_cache_valid any 1s;
  fastcgi_cache_use_stale updating;
  fastcgi_cache_background_update on;
}

...
</code></pre></div></div>

<hr />
<ol>
  <li><a href="https://siipo.la/blog/never-miss-the-cache-with-nginx-microcaching">https://siipo.la/blog/never-miss-the-cache-with-nginx-microcaching</a></li>
</ol>
:ET